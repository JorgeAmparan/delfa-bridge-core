# ============================================================
# DOCYAN LDE — by XCID — Python dependencies (local/dev + B1)
# Actualizado 30 mayo 2026 — B0.5
# ============================================================
# Superset de desarrollo. La imagen Docker usa requirements.docker.txt
# (superficie mínima bootable). Este archivo incluye además el stack de
# ingesta B1+ (Docling/LlamaIndex/Transformers) y reconstrucción de
# formato B4/B5, más paquetes macOS-only de OCR local para dev.
# ============================================================

# Core framework
fastapi==0.135.3
uvicorn==0.42.0
python-dotenv==1.2.2
python-multipart==0.0.22
pydantic==2.12.5
pydantic-settings==2.13.1

# Auth
bcrypt==5.0.0
PyJWT==2.12.1

# Supabase
supabase==2.28.3

# --- post-PoC: arquitectura de ingesta (B1+) ---
graphrag-sdk          # FalkorDB GraphRAG-SDK 1.1.1 — reemplaza DII. Pin en B1.
litellm               # LLM-agnóstico (prefijo gemini/ obligatorio). Pin en B1.
apscheduler           # Scheduler backend Redis (decisión #3). Pin en B1.

# LLM providers
anthropic==0.87.0
google-genai==1.70.0
google-generativeai==0.8.6   # Gemini 2.5 Flash extracción + resolution (ingesta)
openai==2.30.0               # gpt-4o-mini = QA del pipeline de ingesta

# Document processing + stack de ingesta multi-formato (adenda 5) — B1
docling==2.84.0
langextract==1.2.0
llama-index==0.14.19
llama-index-core==0.14.19

# Cotizador pre-ingesta (B3)
tiktoken==0.12.0

# Reconstrucción de formato (B4/B5)
weasyprint
python-pptx==1.0.2
lxml==6.0.2
python-docx==1.2.0
openpyxl==3.1.5
pandas==3.0.2

# Embeddings (BGE-M3 client HTTP) + modelos locales para dev
httpx==0.28.1
transformers==5.4.0          # BGE-M3 / modelos locales (servicio aparte en prod)
sentence-transformers==5.1.2

# Fuentes documentales conservadas (app/ingest_sources/*)
google-api-python-client==2.149.0   # Google Drive
google-auth==2.35.0                  # Google Drive / OneDrive
msal==1.31.1                         # OneDrive (MS Graph)
paramiko==3.5.1                      # FTP/SFTP

# Helpers DB genéricos (app/integrations/sql.py)
SQLAlchemy==2.0.48
PyMySQL==1.1.2

# Graph & cache
falkordb>=1.0.0
redis>=5.0.0

# Billing
stripe==15.0.1

# MCP
mcp==1.27.0
sse-starlette==3.3.4

# Utils
requests==2.33.1
beautifulsoup4==4.14.3
Jinja2==3.1.6
tenacity==9.1.4              # retry rate limiting Gemini
email-validator==2.2.0

# ============================================================
# Binarios externos (B5) — instalar vía apt/git en Dockerfile,
# NO vía pip: vecalign, hunalign (alineadores Pista B).
# Eliminados en B0.5 (conectores retirados): slack-sdk, simple-salesforce.
# ============================================================
