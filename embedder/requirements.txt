# docyan-lde-embedder — dependencias del servicio BGE-M3 (B1 §4).
# Stack pesado (torch ~2-3 GB) que vive AQUÍ, no en el backend principal.
# torch CPU-only: el índice extra evita arrastrar las wheels CUDA (~5 GB).
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.6.0
sentence-transformers==3.3.1
fastapi==0.135.3
uvicorn[standard]==0.42.0
pydantic==2.12.5
