{
  "name": "docyan-lde-core",
  "private": true,
  "description": "DOCYAN LDE — by XCID. Orquestador de scripts raíz (backend Python + frontend Next.js).",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "frontend:dev": "npm run dev --workspace frontend",
    "frontend:build": "npm run build --workspace frontend",
    "frontend:test": "npm run test --workspace frontend",
    "frontend:test:e2e": "npm run test:e2e --workspace frontend",
    "frontend:lint": "npm run lint --workspace frontend",
    "frontend:gen-types": "bash frontend/scripts/generate-types.sh",
    "backend:test": "python -m pytest tests/ -q",
    "backend:lint": "ruff check app/ tests/"
  }
}
